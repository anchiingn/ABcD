import { Modal } from "../../../context/Modal";
import { useDispatch } from "react-redux";
import { thunkFetchUpdateReview } from "../../../store/reviewReducer";

export default function UpdateReview ({ spots }) {
    console.log(spots)
    return (
        <>
            <div>How wwas your stay at</div>
        </>
    )
}